import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer className="flex justify-around items-center mt-4 border-t pt-2">
      {/* You can add footer content here in the future */}
    </footer>
  );
};

export default Footer;
